# World, Action & Reward Models — доучить VLA малым бюджетом демо

## Задание

**100 баллов.** Доучить VLA до новых задач малым бюджетом демонстраций.

### Цель

Современные VLA-модели (vision-language-action) обучаются на больших робо-датасетах
и обещают «обучить один раз, использовать в самых разных задачах». На практике узкое
место — цена адаптации: каждая новая задача требует новых телеоп-демонстраций, а
телеоп дорог и не скейлится. Цель — сдвигать эту кривую: тот же success за меньше
разметки, в пределе из одного лишь видео. Вторая цель — **оценка без реварда**: у
большинства реальных задач нет reward-функции, и мы учим модели измерять прогресс и
качество поведения прямо по видео.

С этого стоит начать: модель, дообученная на десятках задач одного робота в похожих
сценах, на новой задаче будет то справляться с полуслова, то оставаться беспомощной
даже после 25 демонстраций. Куда девается генерализация и что возвращает её дешевле
всего?

Главная метрика задания — **cost curve**: success на новой задаче в зависимости от
числа демонстраций $n \in \{0, 5, 10, 25\}$ (ноль значит zero-shot, по одной языковой
инструкции). Метод адаптации хорош ровно настолько, насколько он сдвигает эту кривую
влево. Правильного решения у задания нет: ваши выборы и их обоснования интересуют
не меньше, чем итоговые числа.

### Контекст

- **LIBERO** — стандартный бенчмарк адаптации VLA к новым задачам; используем через
  официальную интеграцию в LeRobot, там же описана установка; датасет —
  `HuggingFaceVLA/libero`.
- **SmolVLA** ($\approx 450$M) — модель, с которой вы работаете; базовый чекпойнт —
  `lerobot/smolvla_base`, дообучение на LIBERO — ваша Задача 1.
- **Сплит фиксированный**, чтобы решения были сравнимы: seen-часть — `libero_90`
  (можно разумное подмножество, если компьюта мало, но скажите об этом явно),
  целевые held-out задачи — первые три задачи `libero_goal` (`task_ids` 0–2).
- Полезные статьи: **OpenVLA** — адаптация VLA на большем масштабе; **LAPO** —
  латентные действия из видео без разметки (к бонусу A); **TimeRewarder** — сигнал
  прогресса из пассивного видео (к бонусу B); **Robometer** с открытым чекпойнтом
  `Robometer-4B-LIBERO` — готовая reward-модель (к бонусу B).

Пользуйтесь любыми библиотеками, фреймворками и AI-агентами: исходим из того, что
кодинг-агентами вы пользоваться будете, и финальный созвон устроен с учётом этого.

### Задача 1. Seen-претрен, zero-shot и предсказания (20 баллов)

Дообучите `smolvla_base` на seen-части. Есть несколько разумных путей: полный
`libero_90`, его подмножество под ваш компьют, готовый открытый LIBERO-файнтюн, если
найдёте такой. Выберите один и объясните почему.

Измерьте **точку 0**: success вашего seen-чекпойнта на целевых задачах по одной лишь
инструкции. Добавьте **контроль на язык**: прогоните те же эпизоды с инструкцией от
другой задачи. Если success не изменился — модель не читает инструкцию; указать на
это и разобраться ценнее, чем замолчать.

Зафиксируйте **бейзлайн**: наивный файнтюн вашего seen-чекпойнта на 5/10/25 демо
целевой задачи (берите первые по порядку в датасете, не выбирайте удачные), без
ухищрений. Это точка отсчёта для Задачи 2, менять её задним числом нельзя.

До любых дальнейших запусков запишите в отчёт **предсказания**: какой формы будет
ваша итоговая кривая и почему. Несбывшиеся предсказания не штрафуются: их разбор в
Задаче 5 обычно интереснее сбывшихся.

### Задача 2. Ваш метод адаптации (25 баллов)

Побейте свой бейзлайн на каждом из бюджетов 5/10/25. Как — на ваше усмотрение: LoRA
или полный файнтюн, своя голова действий, токенизация и чанкинг действий,
аугментации, претрен-микс из демо seen-задач. Каждый подход — это гипотеза: перед
запуском предскажите, поможет он сильнее при 5 демо или при 25, и проверьте.

**Жёсткие ограничения:** только данные LIBERO (среда нужна для оценки, не для сбора
нового); бюджет демо на целевую задачу строгий (5/10/25, первые по порядку в
датасете); демо seen-задач использовать можно, демо других целевых задач — нельзя.

**Итог задачи** — cost curve $0/5/10/25$, ваш метод против вашего бейзлайна: среднее
по трём целевым задачам, не меньше 20 эпизодов оценки на задачу и точку, минимум два
сида обучения.

### Задача 3. Разбор фейлов (15 баллов)

Поднимите реальные роллауты: три характерных провала с кадрами или видео. Для
каждого — ваша гипотеза, что именно ломается, и дизайн эксперимента, который эту
гипотезу отделил бы от альтернативной. Проводить эти эксперименты не обязательно,
оценивается дизайн.

### Задача 4. Бонус на выбор (20 баллов, выберите один)

- **A. Action-free данные.** Соберите пул сами: возьмите демо seen-задач и не
  используйте их действия, только видео. Можно ли извлечь из такого пула пользу для
  адаптации: латентные действия, inverse dynamics, представления? Это предельная
  форма всей постановки задания — учиться действовать из видео без разметки.
- **B. Оценка без реварда.** Выучите по видео собственный сигнал прогресса и
  отранжируйте им свои чекпойнты; сравните с готовой `Robometer-4B-LIBERO`: кто
  ранжирует ближе к настоящему success — ваш маленький критик или 4B-фаундейшен? И
  отдельно: если оптимизировать политику прямо на выученный сигнал, она станет лучше
  или научится его обманывать?
- **C. Длинный горизонт.** LIBERO-Long: многостадийные задачи. Помогает ли
  декомпозиция на сабтаски и планирование поверх них там, где плоский файнтюн
  ломается?

### Задача 5. Summary и обсуждение (20 баллов)

Три пункта, в любом порядке:

- **Предсказания против результатов:** где вы ошиблись и что это говорит о ваших
  исходных представлениях?
- **Кладбище идей:** два-три подхода, которые вы рассмотрели и отвергли, и почему.
- **Самое неожиданное наблюдение** за время работы и ваше объяснение.

### Что нужно сдать

1. Запускаемый репозиторий с кодом и README: setup, точные команды воспроизведения
   каждой задачи. Чистота и воспроизводимость кода — часть оценки.
2. Отчёт, **максимум 4 страницы**: предсказания (записанные до запусков), cost curve,
   разбор фейлов, результаты бонуса, ответы Задачи 5. Лимит жёсткий: читать будут
   ровно четыре страницы, остальное складывайте в приложение к репозиторию.

---

## Сетап

| | |
|---|---|
| Модель | `lerobot/smolvla_base` ($\approx 450$M) |
| Seen | `libero_90` (или явно описанное подмножество) |
| Target | `libero_goal`, `task_ids` 0–2 |
| Бюджеты демо | $0 / 5 / 10 / 25$, **первые по порядку** в датасете |
| Оценка | $\ge 20$ эпизодов на задачу и точку, $\ge 2$ сида обучения |
| Датасет | `HuggingFaceVLA/libero` |

Локально (macOS) — только инспекция датасета и графики. Обучение и оценка в среде
LIBERO — на GPU-машине, см. `setup_gpu.sh` (LIBERO требует Linux, `MUJOCO_GL=egl`).

### На что напороться (проверено по `ext/lerobot`)

1. **`libero_90` нет ни в `HuggingFaceVLA/libero`, ни в `lerobot/libero`.** Оба —
   1693 эпизода / 40 задач, то есть только spatial + object + goal + long. Seen-часть
   придётся брать отдельно: `IPEC-COMMUNITY/libero_90_no_noops_lerobot` (v2.1, 3921
   эп.), `crislmfroes/libero_90_lerobot` (v3.0, 4500 эп.), `jesbu1/libero_90_lerobot`.
   Выбор датасета и его формат — явно в отчёт.
2. **Датасет для целевых задач лучше брать `lerobot/libero`**: те же демо, что в
   `HuggingFaceVLA/libero`, но 1.9 GB против 69.9 GB (видео вместо PNG-в-паркете).
   Требует `torchcodec` или `pyav`.
3. **Пинить ревизию**: `--dataset.revision=<sha>`, иначе числа несравнимы между
   перезаливками хаба.
4. **Контроль на язык не делается флагом.** У `LiberoEnv` есть `task`, `task_ids`,
   но нет поля с текстом инструкции — подмена инструкции требует своего eval или
   патча обёртки среды. Заложить время.
5. **`task_ids` — это порядок в сьюте, а не `task_index` в датасете.** Сверить
   инструкцию из `demo_subset.py --list` с той, что реально крутит среда, до того как
   набирать демо.
6. **Контроль-мод**: `--env.control_mode=relative|absolute` должен совпадать с тем,
   на чём обучалась политика.

## Предсказания (записать ДО запусков)

<!-- форма кривой и почему; потом это разбирается в Задаче 5 -->

## Лог

**2026-08-16 — сетап, работа с датасетом локально.**

- LeRobot **ставится на macOS** без LIBERO: `pip install -e "ext/lerobot[dataset]"`
  (extra `libero` тянет `hf-libero` только под Linux). Версия 0.6.2. То есть датасет,
  выбор демо и графики делаются локально, а на GPU-бокс уезжает только обучение и
  оценка в симуляторе.
- **У эпизодов в v3.0 нет колонки с задачей.** `meta.episodes` содержит только
  индексы, длины и ссылки на видео; `task_index` лежит в самих данных. `demo_subset.py`
  строит карту «эпизод -> задача» по паркетам (≈1 минута на 377 файлов) и кеширует её
  в `datasets/`.
- **Эпизоды не сгруппированы по задачам.** У задачи 10 («put the bowl on the plate»)
  49 эпизодов, первые пять — `[379, 422, 426, 431, 433]`. Наивное «первые N строк
  датасета» дало бы совсем другую выборку.
- Раскладка задач в `lerobot/libero`: **0–9 libero_10 (long), 10–19 libero_goal,
  20–29 libero_object, 30–39 libero_spatial**. Целевые задачи задания — первые три
  `libero_goal`, то есть в датасете это индексы около 10–12, но порядок сьюта задаёт
  пакет LIBERO, и это надо сверить на GPU-боксе перед набором демо.
- Ветка `baseline.py eval` проверена без симулятора (`tmp/test_eval_path.py`): разбор
  `eval_info.json`, строка в `runs/results.jsonl`, запись в MLflow. Тестовые данные
  удалены, `results.jsonl` пуст.

## Идеи

## Результаты

| демо | baseline (mean ± std) | ours (mean ± std) |
|---|---|---|
| 0 | | |
| 5 | | |
| 10 | | |
| 25 | | |

## Открытые вопросы

<!-- что не выяснено; убирать отсюда только вместе с ответом в «Лог» -->

- Seen-претрен на libero_90 не сделан: датасета нет в `lerobot/libero`, нужен отдельный
  (`IPEC-COMMUNITY/libero_90_no_noops_lerobot` или `crislmfroes/libero_90_lerobot`).
  Без него «точка 0» из задания не считается.
- Контроль на язык: у `LiberoEnv` нет поля с текстом инструкции, нужен свой eval или
  патч обёртки. Не сделано.
- Полная кривая требует 3 задачи × 2 сида × 4 бюджета — локально это сутки. Решить,
  что уходит на удалённую GPU через ClearML.
- Сколько шагов обучения честно для 5 демо? Взято 1500 без обоснования; переобучение
  не проверялось.

**2026-08-18 — LIBERO поднят на macOS в обход Linux-маркера.**

`hf-libero` объявлен linux-only не из-за симулятора, а из-за `hf-egl-probe` —
C-расширения для выбора GPU при headless-рендере через EGL. На Darwin robosuite
сам ставит `MUJOCO_GL=cgl` и EGL не трогает, то есть зависимость лишняя. Ставится так
(`tmp/install_libero_macos.sh`): все зависимости руками, `robosuite` и `hf-libero`
через `--no-deps`, `egl_probe` пропускаем. Импортируются все пять пакетов.

Первый запуск требует `~/.libero/config.yaml`, иначе пакет спрашивает путь
интерактивно и падает с `EOFError` в неинтерактивной сессии. Конфиг пишется скриптом.

**Мапинг задач получен и он не такой, как я предполагал:**

| `--env.task_ids` | `task_index` в датасете | инструкция |
|---|---|---|
| 0 | **19** | open the middle drawer of the cabinet |
| 1 | **17** | put the bowl on the stove |
| 2 | **14** | put the wine bottle on top of the cabinet |

Заглушка `(10, 11, 12)` в `run_baseline.sh` была неверной — это task_id 8, 9 и 3.
С ней бейзлайн обучался бы на демо от чужих задач и вся кривая была бы мусором.
Проверять этот мапинг перед каждой сдачей: `python demo_subset.py --suite libero_goal`.

Первые 5 демо целевой задачи 0: `[399, 405, 410, 421, 437]`.

**Рендер на macOS работает.** `MUJOCO_GL=cgl`, обе камеры отдают нормальные кадры
(`runs/libero_macos_frame.png`). Скорость чистой среды без политики — **20.3 шага/с**
при рендере 256×256 двух камер.

Что из этого следует по бюджету времени (эпизод libero_goal до 300 шагов):

| что | локально на M1 |
|---|---|
| один эпизод, только среда | ~15 с |
| один эпизод со SmolVLA на MPS | ~30–45 с (оценка, померить) |
| одна точка кривой (3 задачи × 20 эпизодов) | ~30–45 мин |
| вся cost curve (4 бюджета × 2 сида) | 4–6 ч только на оценку, без обучения |

**Вывод:** диагностика, zero-shot и разбор фейлов делаются локально. Полная кривая с
обучением — на удалённую GPU. То есть Linux нужен не для запуска, а для скорости.

**2026-08-18 — `lerobot-eval` со SmolVLA прошёл целиком на macOS.**

Две правки понадобились:

1. `zsh` съедает `--env.task_ids=[0]` как глоб — аргумент надо кавычить. В
   `run_baseline.sh` это не проблема (там bash), но при ручном запуске из терминала
   ломается с невнятным «no matches found».
2. **Имена камер не совпадают.** `smolvla_base` ждёт `observation.images.camera1/2/3`,
   а LIBERO отдаёт `observation.images.image` и `image2`. Лечится
   `--rename_map='{"observation.images.image": "observation.images.camera1",
   "observation.images.image2": "observation.images.camera2"}'`. Третью камеру
   политика переживает без замены.

**Скорость: эпизод за 26 с (36 шагов/с с политикой на MPS).** Это меняет план: точка
кривой (3 задачи × 20 эпизодов) считается за ~26 минут, вся кривая
(4 бюджета × 2 сида) — около 3.5 часов оценки. Локально это делается за ночь.

Zero-shot `lerobot/smolvla_base` на libero_goal task 0: **0% из 1 эпизода** — ожидаемо,
базовый чекпойнт LIBERO не видел. Настоящая точка 0 требует seen-претрена.

**Скорости на M1 (замерено 2026-08-18, под конкуренцией с тремя другими задачами):**

| операция | скорость |
|---|---|
| среда без политики | 20 шагов/с |
| оценка со SmolVLA на MPS | 24–36 шагов/с, эпизод ~26 с |
| **обучение SmolVLA на MPS** | **1.9 с/шаг при batch 2** |

То есть локально реально и обучать: 1500 шагов ≈ час. Полная кривая на трёх задачах
с двумя сидами — это уже сутки, её на удалённую GPU; но одну задачу с сеткой приёмов
можно перебрать за ночь.

Дефолт SmolVLA уже консервативный: `freeze_vision_encoder=true`,
`train_expert_only=true`. То есть «наивный файнтюн» из условия по факту и есть
дообучение только экспертной головы. Полное размораживание — отдельный вариант сетки.

## Лог экспериментов

**Точка 0 (zero-shot базового чекпойнта).** `lerobot/smolvla_base` без всякого
дообучения, 20 эпизодов на задачу, `--env.init_states=true`, eval-seed 1000:

| задача | success |
|---|---|
| libero_goal task 0 (open the middle drawer) | 0.00 |

Ожидаемо: базовый чекпойнт LIBERO не видел. Это **не** та «точка 0», которую просят в
задании — там нужен seen-претрен на libero_90. Но она полезна как абсолютный ноль:
всё, что выше, дал именно претрен, а не случайность.

**Грабли, стоившие 20 эпизодов впустую:** оценка считалась успешно, но строка в
`results.jsonl` не писалась — код искал метрики под ключом `aggregated`, а lerobot
кладёт их под `overall`. Числа удалось поднять с диска (`tmp/recover_rows.py`), но
урок общий: **после первого прогона проверять, что строка появилась**, а не только
что процесс завершился с нулевым кодом.

### Ошибки Metal при обучении на MPS — результаты можно молча потерять

Обучение SmolVLA на MPS с `batch_size=4` выдало **10876** сообщений вида

```
Error: command buffer exited with error status.
Ignored (for causing prior/excessive GPU errors)
```

начиная с первого же шага. «Ignored» здесь значит, что Metal **отбрасывает** пачки
вычислений, но процесс продолжает идти, tqdm рисует прогресс, чекпойнт сохраняется.
То есть прогон выглядит успешным и даёт числа, которым нельзя верить.

Что при этом чисто:

| прогон | batch | ошибок |
|---|---|---|
| обучение SmolVLA | 4 | **10876** |
| обучение SmolVLA (проба) | 2 | 0 |
| оценка SmolVLA | — | 0–1 |
| обучение looped-модели | 4 | 0 |

То есть дело не в MPS вообще, а в связке «крупная модель + batch 4». Скорее всего сюда
же относится и невоспроизводимый NaN в проекте по интерпретируемости.

**Правило:** после каждого прогона на MPS проверять
`grep -c "command buffer exited with error" лог`. Если не ноль — числа выбросить.
Обучение SmolVLA локально гонять с `batch_size=2` и по одному процессу на MPS,
серьёзные прогоны — на CUDA.

**Первая точка кривой получена локально.** `trick_default` — дефолтный рецепт SmolVLA
(замороженный визуальный энкодер, обучается только экспертная голова), 1500 шагов,
batch 2, 5 демо задачи 0, оценка на 20 эпизодах:

| метод | демо | success |
|---|---|---|
| `zeroshot_base` | 0 | 0.00 |
| `trick_default` | 5 | **0.40** |

Пять демонстраций поднимают задачу с нуля до 40%. Обучение чистое, ошибок Metal ноль.

### Сетка приёмов на 5 демо, задача 0 (2026-08-18)

1500 шагов, batch 2, seed 0, оценка на 20 эпизодах. Все прогоны чистые, ошибок Metal ноль.

| вариант | что меняли | success |
|---|---|---|
| `zeroshot_base` | ничего, базовый чекпойнт | 0.00 |
| `trick_default` | дефолт SmolVLA: энкодер заморожен, учится экспертная голова | 0.40 |
| `trick_unfrozen` | `freeze_vision_encoder=false`, `train_expert_only=false` | **0.45** |
| `trick_lora` | LoRA r=32 | не запустился |

LoRA упала на отсутствующем пакете `peft` — он не входит ни в `[smolvla]`, ни в
`[dataset]`, ставится отдельно. Добавлен в `requirements.txt`.

**Полное размораживание не хуже заморозки (0.45 против 0.40).** Это против ожидания:
на пяти демонстрациях полный файнтюн 450M-модели должен был переобучиться сильнее.
Разница в 0.05 — это один успешный эпизод из двадцати, то есть внутри шума; честный
вывод пока «разницы не видно», и нужны сиды.

### Кривая пошла ВНИЗ — и это дефект моего дизайна, а не метода

Задача 0, сид 0, 20 эпизодов, дефолтный рецепт:

| демо | success |
|---|---|
| 0 | 0.00 |
| 5 | **0.40** |
| 10 | 0.30 |
| 25 | **0.15** |

Больше демонстраций — хуже результат. Это противоречит всему, чего ждёшь от cost
curve, и причина, похоже, в том, как я поставил эксперимент.

**Число шагов зафиксировано на 1500 при всех бюджетах.** При batch 2 это 3000
сэмплов. Пять демо это примерно 750 кадров, то есть модель проходит по ним **четыре
раза**. Двадцать пять демо — около 3750 кадров, то есть **меньше одного прохода**.
Прогон на 25 демо просто недообучен, а не переобучен.

Это ровно тот случай, когда «метод плохо работает» на самом деле означает «сравнение
нечестное». Правильные варианты, любой на выбор, но выбрать надо **до** прогонов:

1. фиксировать число **эпох**, а не шагов;
2. масштабировать шаги пропорционально числу демо;
3. подбирать число шагов по отложенной части демо для каждого бюджета отдельно.

Третий честнее всего, но требует отложить часть из и без того крошечного бюджета.
Второй самый простой и его я бы и взял, явно оговорив в отчёте.

LoRA (r=32, 5 демо) дала **0.00** — при том же числе шагов она обучается заметно
медленнее полного файнтюна, так что здесь тот же дефект, только сильнее.

**Вывод для отчёта:** первую же полученную кривую пришлось выбросить из-за
confound'а в собственном дизайне. Записываю как есть — в задании прямо просят
разбирать несбывшиеся предсказания.

### Разбор роллаутов: подъезд выучен, финальная манипуляция нет

`tmp/diag_rollouts.py` собирает кадры из провальных эпизодов в контактный лист.
80 эпизодов, 65 провалов. Четыре характерных провала на задаче «open the middle
drawer of the cabinet» дают один и тот же паттерн:

рука уверенно доезжает до шкафа и зависает над ящиком — **и на этом всё**. Захвата
за ручку и вытягивания не происходит ни разу за 300 шагов.

То есть из пяти-десяти демонстраций выучивается **грубое движение к правильному
месту**, а точный захват — нет. Это согласуется с тем, что дефолтный рецепт
замораживает визуальный энкодер и учит только экспертную голову: голова осваивает
крупную траекторию, а тонкая координация «схват относительно ручки» требует либо
большего числа демо, либо дообучения зрения.

**Гипотеза и как её отделить:** если дело в зрении, то размораживание энкодера должно
чинить именно финальную фазу. У нас `trick_unfrozen` дал 0.45 против 0.40 — в шуме,
но проверять надо не success, а **долю эпизодов, дошедших до контакта с ручкой**.
Такая метрика различит «не доезжает» и «доезжает, но не хватает» гораздо надёжнее,
чем бинарный успех, и стоит того, чтобы её посчитать.

### Кривая после починки бюджета: монотонная

Шаги пропорциональны числу демо (300 на демо), всё остальное как было:

| демо | при 1500 шагах на все бюджеты | при 300 шагах на демо |
|---|---|---|
| 0 | 0.00 | 0.00 |
| 5 | 0.40 | 0.40 |
| 10 | **0.30** | **0.60** |
| 25 | **0.15** | **0.70** |

Диагноз подтвердился полностью: падающая кривая была артефактом недообучения на
больших бюджетах, а не свойством метода. После починки форма нормальная и
насыщающаяся — резкий скачок на первых пяти демо, дальше пологий рост.

Это ровно то, что в задании называют «предсказания против результатов»: я предсказал
падение из-за переобучения, получил падение из-за **недо**обучения, и различить их
можно было только посмотрев, сколько раз модель проходит по данным.


## Контроль на язык: политика инструкцию читает, но не только её (2026-08-20)

`language_control.py`. Тот же чекпойнт `fixed_n25` (25 демо задачи 0), те же 20
эпизодов, те же начальные состояния и сид — подменена только инструкция, которую
читает политика: вместо «put the white mug on the left plate and put the yellow and
white mug on the right plate» подставлена инструкция задачи 3, «turn on the stove and
put the moka pot on it».

| инструкция | success |
|---|---|
| своя | **0.70** |
| чужая | **0.20** |

Это ответ на пункт задания «если success не изменился, модель не читает инструкцию».
Он изменился, и сильно — падение в 3.5 раза. Значит язык в политике работает.

Но 0.20 — это не ноль, и вот это интереснее. Пятая часть успехов достаётся вообще без
правильной инструкции. Объяснение, которое я считаю основным: сцена в LIBERO сама по
себе задаёт задачу. На столе стоят ровно те объекты, которые нужны, и политика,
дообученная на 25 демо ровно этой сцены, воспроизводит нужную траекторию по картинке.
То есть часть «понимания задачи» на самом деле лежит не в языке, а в узнавании сцены.

Отсюда важное для интерпретации нулевой точки кривой: zero-shot success базового
чекпойнта на этих задачах равен 0.0, и это **не** значит, что модель не понимает
инструкцию — она просто не умеет действовать в этой сцене. Разделить эти два эффекта
можно было бы, прогнав ту же проверку на чекпойнте, обученном на нескольких задачах
сразу; на одной задаче они неразличимы.


## Предсказания по приёмам, записаны до запуска (2026-08-20)

Условие требует записывать предсказание до прогона, поэтому здесь, до запуска
`./run_tricks.sh`. Все приёмы — на 5 демо задачи 0, 1500 шагов, batch 2, тот же сид.
Точка отсчёта — `trick_default` 0.40 и `trick_unfrozen` 0.45.

| приём | что делает | предсказание |
|---|---|---|
| `trick_lora` | LoRA r=32 вместо полного файнтюна | 0.35–0.45, **не хуже дефолта**; сильнее поможет на 5 демо, чем на 25 — при малом числе демо ограничение ёмкости работает как регуляризация, при 25 оно просто мешает |
| `trick_mix` | 5 демо цели + 40 демо seen-задач | **лучше всего на 5 демо**: главный враг при пяти демо — забывание того, что умел базовый чекпойнт, а подмешивание держит его на месте. Жду 0.5–0.6. На 25 демо выигрыш должен почти исчезнуть |
| `trick_chunk` | chunk_size 50 -> 10 | скорее хуже: короткий чанк чаще перепланирует и должен быть устойчивее к сносу, но SmolVLA обучался с 50, и уменьшение ломает согласование с претреном. Жду 0.2–0.4 |
| `trick_aug` | стандартные аугментации изображения | почти без изменения на этой задаче: сцена и камера фиксированы, вариативности, к которой готовят аугментации, в тесте нет. Жду ±0.05 |

Общая гипотеза, которую эти четыре проверяют: при пяти демо мы ограничены **не
ёмкостью**, а тем, что дообучение сносит базовый чекпойнт. Тогда всё, что удерживает
его на месте (LoRA, подмешивание), помогает, а всё, что просто добавляет
вариативности данных (аугментации), — нет.

### Про seen-часть: libero_90 в этом датасете нет

`lerobot/libero` содержит 40 задач: libero_10 (0–9), libero_goal (10–19),
libero_object (20–29), libero_spatial (30–39). Задач libero_90 в нём нет вовсе.
Поэтому подмешивание идёт демо **libero_object** — это не целевые задачи и не
остальные задачи libero_goal, то есть ограничение «демо других целевых задач нельзя»
соблюдено. Отступление от буквы условия (там seen — это libero_90) записано здесь и
пойдёт в отчёт.


## Подмешивание seen-демо: первый заход мерил не то (2026-08-20)

| прогон | демо | шагов | эпох по своим данным | финальный лосс | success |
|---|---|---|---|---|---|
| `trick_default` | 5 целевых | 1500 | **4.11** | 0.267 | **0.40** |
| `trick_mix` | 5 целевых + 40 seen | 1500 | **0.43** | 0.427 | **0.00** |

Предсказание было 0.5–0.6, получилось 0. Разбор: ошибок Metal ноль, обучение прошло
штатно, но при том же числе шагов и вдевятеро большем пуле **целевые демо получили в
девять раз меньше показов** — 0.43 эпохи против 4.11. Это ровно тот же конфаунд, что
уже ломал кривую цены: при фиксированном числе шагов больше данных означает меньше
проходов по каждому.

То есть измерено «что будет, если разбавить целевые демо», а не «помогает ли
подмешивание». Первое мы и так знали.

**Переделано.** `trick_mix2`: 5 целевых + 5 seen (по одному демо на задачу
libero_object) и вдвое больше шагов. Тогда доля целевых данных 1/2, шагов 3000, и
целевые демо видны ровно столько же раз, сколько в бейзлайне: $3000\times 2\times
\tfrac12 = 1500\times 2$. Различие с бейзлайном ровно одно — половина батча приходит
не из целевой задачи.

Прогон `trick_mix` оставлен в `results.jsonl` как есть: он честно меряет цену
разведения, и в отчёт пойдёт как отдельная строка, а не как неудачная попытка.


## LoRA недоучивается на том же бюджете шагов (2026-08-20)

Теперь с установленным peft: в логе `Wrapped smolvla with PEFT (LoraConfig)`, ошибок
Metal ноль, 4.11 эпохи по пяти демо — то есть прогон технически валиден.

| прогон | финальный лосс | success |
|---|---|---|
| `trick_default` (полный файнтюн) | **0.267** | 0.40 |
| `trick_lora` (r=32, тот же lr 1e-4) | **0.521** | **0.00** |

Лосс вдвое выше, то есть дело не в «LoRA хуже обобщает», а в том, что она **не
доехала**. Это ожидаемо и известно: у LoRA обучаемых параметров на порядки меньше, и
шаг оптимизатора, подобранный под полный файнтюн, для неё слишком мал — обычная
практика брать для LoRA learning rate в 5–10 раз выше.

Значит сравнение при равных шагах **и равном lr** заведомо не в её пользу, и в таком
виде оно ничего про метод не говорит. Предсказание «0.35–0.45, не хуже дефолта» было
сделано в предположении, что lr общий подойдёт; это предположение и оказалось
неверным, а не сама идея.

**Что доделать:** один прогон `--peft.method_type=LORA --optimizer.lr=1e-3` при том же
числе шагов. Если лосс сядет на уровень дефолта, а success нет — тогда это уже
свойство метода. Поставлено в очередь после `mix2 / chunk / aug`.


## Подмешивание seen-демо удваивает success (2026-08-20)

| прогон | данные | шагов | эпох | лосс | **success** |
|---|---|---|---|---|---|
| `trick_default` | 5 целевых | 1500 | 4.11 | 0.267 | **0.40** |
| `trick_unfrozen` | 5 целевых | 1500 | — | — | 0.45 |
| `trick_mix` | 5 целевых + 40 seen | 1500 | 0.43 | 0.427 | 0.00 |
| **`trick_mix2`** | 5 целевых + 5 seen | 3000 | 4.32 | **0.218** | **0.80** |

Ошибок Metal ноль. Предсказание было 0.5–0.6, получилось 0.80 — угадал направление,
недооценил величину. Лосс тоже ниже, 0.218 против 0.267, притом что данные сложнее:
две разные задачи вместо одной.

Это согласуется с гипотезой, которую я записывал до запусков: при пяти демо мы
ограничены не ёмкостью, а тем, что дообучение сносит базовый чекпойнт. Половина
батча из чужой задачи держит его на месте и работает как регуляризация.

### Контроль пройден: выигрыш от подмешивания, а не от лишних шагов

| прогон | данные | шагов | эпох | лосс | **success** |
|---|---|---|---|---|---|
| `trick_default` | 5 целевых | 1500 | 4.11 | 0.267 | 0.40 |
| `trick_default3000` | 5 целевых | 3000 | 8.80 | **0.155** | 0.50 |
| **`trick_mix2`** | 5 целевых + 5 seen | 3000 | 4.32 | 0.218 | **0.80** |

Удвоение шагов само по себе даёт 0.40 → 0.50. Подмешивание при том же числе шагов —
0.50 → **0.80**. То есть большая часть выигрыша действительно от подмешивания.

И вот что важнее самого числа: у `trick_default3000` **лосс ниже** (0.155 против
0.218), а success **хуже** (0.50 против 0.80). Ниже обучающий лосс при худшем
поведении — это подпись переобучения, и она подтверждает гипотезу, записанную до
запусков: при пяти демо мы упираемся не в ёмкость, а в то, что дообучение сносит
базовый чекпойнт. Половина батча из чужой задачи держит его на месте.

При 20 эпизодах 0.50 против 0.80 — это 10 успехов против 16, разрыв 0.30 при
разрешении 0.05. Для промежуточного прогона достаточно, полный прогон на трёх
задачах и двух сидах — на A100.

### Как контроль был устроен

У `trick_mix2` вдвое больше шагов оптимизатора. Показов целевых демо столько же
($3000\times 2\times \tfrac12 = 1500\times 2$), но общий бюджет оптимизации удвоен.
Поэтому запущен `trick_default3000`: те же пять целевых демо, ничего не подмешано,
3000 шагов.

- если он тоже даёт около 0.8 — дело в числе шагов, а не в подмешивании;
- если остаётся около 0.4 или падает от переобучения — дело в подмешивании.

Без этого числа заголовок «подмешивание удваивает success» писать нельзя.


## Чанкинг действий: короче не значит лучше

`trick_chunk` (chunk_size и n_action_steps 50 → 10): **0.15** против 0.40 у дефолта.
Предсказание было 0.2–0.4, то есть «скорее хуже» — сбылось.

Объяснение то же, что и в предсказании: SmolVLA претренирован с чанком 50, и
уменьшение ломает согласование с претреном. Короткий чанк чаще перепланирует, но эта
выгода не окупает рассогласования — по крайней мере при пяти демо, когда
переучить голову действий не на чем.


## Все приёмы на пяти демо: предсказания против результатов (2026-08-20)

Один сид, задача 0, 20 эпизодов, 1500 шагов кроме отмеченных. Ошибок Metal нигде.

| приём | предсказание | **success** | сбылось |
|---|---|---|---|
| дефолт (зрение заморожено) | — | 0.40 | — |
| полное размораживание | — | 0.45 | — |
| **подмешивание seen, 3000 шагов** | 0.5–0.6 | **0.80** | направление да, величина занижена |
| контроль: 3000 шагов без подмешивания | — | 0.50 | — |
| чанк 50 → 10 | 0.2–0.4 | 0.15 | да |
| аугментации изображения | ±0.05 | **0.10** | **нет** |
| LoRA r=32, общий lr | 0.35–0.45 | 0.00 | нет, недоучилась |

### Аугментации: предсказание не сбылось, и объяснение содержательное

Я ждал «почти без изменений: сцена и камера фиксированы, вариативности, к которой
готовят аугментации, в тесте нет». Получилось **0.10 против 0.40** — вчетверо хуже.

Чего я не учёл: в дефолте SmolVLA **зрительный энкодер заморожен**. Аугментация
сдвигает вход в распределение, которого замороженный энкодер не видел, а
подстроиться под сдвиг ему нечем — обучается только голова действий. Плюс при пяти
демо сигнал и так на пределе, и добавленная дисперсия его добивает.

Проверяемое следствие: аугментации должны перестать вредить (и, может быть, начать
помогать) при `--policy.freeze_vision_encoder=false`, где энкодеру есть чем
поглотить сдвиг. Это отдельный дешёвый прогон.

### Что из этого стоит везти на A100

Единственный приём, который дал улучшение сверх контроля, — **подмешивание демо
seen-задач**. Его и надо гонять на полном протоколе: три целевые задачи, два сида,
бюджеты 5/10/25. Остальное на этом бюджете отбраковано, и отбраковано с объяснением,
а не по одному числу.


## Флаг learning rate молча не сработал

Перезапустил LoRA с `--optimizer.lr=1e-3` — лосс остался 0.523, ровно как при
дефолтном шаге. Проверил дамп конфига: `'optimizer_lr': 0.0001`. **Флаг был принят и
проигнорирован.**

Причина в устройстве конфига lerobot: оптимизатор строится из пресета политики,
`SmolVLAConfig.get_optimizer_preset()` берёт `lr=self.optimizer_lr`, и верхнеуровневый
`optimizer.lr` этим пресетом перекрывается. Рабочий флаг — `--policy.optimizer_lr`.

Ошибка из тех, что не падают: команда отработала, лосс правдоподобный, число в
таблицу попало бы как «LoRA не работает даже с поднятым шагом». Проверять надо не
факт запуска, а дамп конфига — он печатается в начале лога.

### Оба флага не работают, и проверять надо не конфиг, а лог обучения

`--policy.optimizer_lr=1e-3` тоже не применился. Сравнение **эффективного** шага по
логам на одинаковых номерах сэмплов:

| прогон | smpl:400 | smpl:800 |
|---|---|---|
| дефолтный LoRA | `lr:8.7e-05` | `lr:9.0e-05` |
| с `--policy.optimizer_lr=1e-3` | `lr:8.7e-05` | `lr:9.0e-05` |

Байт в байт. Причина в `configs/train.py:312`: при `use_policy_training_preset=true`
(дефолт) оптимизатор и планировщик **безусловно перезаписываются** пресетом политики,
а пресет берёт `optimizer_lr` из конфига, загруженного вместе с весами по
`--policy.path`. Оба флага принимаются молча и выбрасываются.

**Отсюда правило проверки:** смотреть не на факт запуска и даже не на дамп конфига, а
на эффективную величину в строках обучения. Дамп в этом случае тоже показывал 0.0001,
но он мог бы показывать и правильное значение при неправильном поведении.

Рабочий путь — тот, что описан в самом коде: отключить пресет и задать обе части явно.

```
--use_policy_training_preset=false \
--optimizer.type=adamw --optimizer.lr=1e-3 --optimizer.grad_clip_norm=10 \
--scheduler.type=cosine_decay_with_warmup --scheduler.peak_lr=1e-3 \
--scheduler.decay_lr=1e-5 --scheduler.num_warmup_steps=100 \
--scheduler.num_decay_steps=1500
```

Проверено, что обходной путь действительно работает:

| прогон | smpl:400 | smpl:800 |
|---|---|---|
| пресет политики (флаг выброшен) | `lr:8.7e-05` | `lr:9.0e-05` |
| пресет отключён, шаг задан явно | **`lr:7.4e-04`** | **`lr:9.0e-04`** |

Ровно в десять раз больше — шаг наконец тот, что просили. `run_tricks.sh` переписан на
рабочий набор флагов.

### Ещё одна ошибка гигиены, из-за которой я чуть не сравнил прогон сам с собой

Три попытки подряд писали в **один и тот же** `tmp/trick_lora_lr_train.log`, и пока
предыдущая оболочка доживала свой шаг оценки, файл перезаписывался. Сравнение «лосс
такой же, значит флаг не сработал» едва не оказалось сравнением прогона с самим собой.
Логи параллельных попыток должны иметь разные имена, иначе вывод делается по чужим
числам.


## LoRA с правильным шагом: работает, но полный файнтюн не догоняет

| прогон | эффективный lr | лосс | **success** |
|---|---|---|---|
| полный файнтюн | 9.0e-05 | **0.267** | **0.40** |
| LoRA r=32, шаг выброшен пресетом | 9.0e-05 | 0.521 | 0.00 |
| LoRA r=32, шаг задан в обход пресета | 9.0e-04 | 0.314 | **0.30** |

Шаг оптимизатора отвечал за всё различие между «метод не работает вообще» и «метод
работает, но чуть хуже». Ноль был артефактом недообучения, а не свойством LoRA.

С правильным шагом LoRA почти догоняет полный файнтюн по лоссу (0.314 против 0.267) и
отстаёт по success (0.30 против 0.40). Предсказание было «0.35–0.45, не хуже дефолта»
— по величине близко, по знаку неверно.

**Чего это не доказывает.** Это одна точка в двумерной сетке: ранг и шаг у LoRA
связаны, и r=32 при 1e-3 — не оптимум, а первое разумное значение. Утверждать можно
только «LoRA при r=32 и lr=1e-3 на пяти демо не бьёт полный файнтюн», а не «LoRA не
работает». Сетка по рангу — на A100, здесь она не нужна.

## Итоговая таблица приёмов

| приём | предсказание | success | вывод |
|---|---|---|---|
| дефолт | — | 0.40 | точка отсчёта |
| полное размораживание | — | 0.45 | в пределах разрешения |
| **подмешивание seen (равная экспозиция)** | 0.5–0.6 | **0.80** | **единственное настоящее улучшение** |
| контроль: те же шаги без подмешивания | — | 0.50 | отделяет шаги от подмешивания |
| LoRA r=32, свой шаг | 0.35–0.45 | 0.30 | работает, но хуже |
| чанк 50 → 10 | 0.2–0.4 | 0.15 | ломает согласование с претреном |
| аугментации | ±0.05 | 0.10 | вредят при замороженном энкодере |
| подмешивание без равной экспозиции | — | 0.00 | измеряло разведение, а не приём |

На A100 везём **подмешивание**: три целевые задачи, два сида, бюджеты 5/10/25.
